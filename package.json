{
  "name": "streamed-flux",
  "version": "0.0.1",
  "description": "demonstration to build the flux patterns by stream.",
  "dependencies": {
    "deepmerge": "^0.2.7",
    "event-stream": "^3.3.0",
    "express": "^4.12.3",
    "hjs": "0.0.6",
    "printf": "^0.2.1",
    "react": "^0.12.2",
    "shoe": "0.0.15"
  },
  "devDependencies": {
    "reactify": "^1.1.0"
  },
  "scripts": {
    "build": "browserify -t reactify ./app/main.js -o ./public/js/bundle.js",
    "start_app": "NODE_PATH=lib node app.js &",
    "start": "npm run build && npm run start_app"
  }
}
